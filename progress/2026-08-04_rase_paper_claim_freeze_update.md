# Paper claim freeze update — 2026-08-04

Supersedes selector-centric claims in
`progress/2026-07-31_paper_claim_freeze.md` only where PRE evidence changes the
method branch. Benchmark claim boundaries from W9C remain in force.

## Active paper posture

**Benchmark / diagnosis** is the only currently claimable track.

Conditional method track (**safe handback / calibrated termination**) stays
gated on PRE-A3 hidden-test pass.

## Claims allowed

1. Recoverability is policy-relative: frozen SmolVLA can fail where OpenVLA-OFT
   succeeds on the same snapshot (W7/W8; Goal/Long strongest).
2. Direct escalation is a deployable recovery action; prefix portfolio is not
   significantly better than direct OFT on the W7/W8 paired set.
3. Same-policy resampling and short expert prefixes do not create rescue
   opportunity on the PRE-A0/A1 development audits.
4. Recovery duration has structure on a development pilot (PRE-A2), but is not
   yet confirmatory.
5. Learned three-arm ridge selector fails its preregistered matched-random gate
   (W9C).

## Claims forbidden

1. “We learned when to escalate” / task-general selector success.
2. Candidate ranking novelty without heterogeneous unique successes.
3. Timing-selector novelty (Phase 0 gates failed).
4. Suite-general recoverability including Object/Spatial (W10 negative).
5. Confirmed safe-handback method gains before PRE-A3 hidden pass.
6. World-model necessity or contribution.

## Main-text structure (benchmark track)

1. Problem: frozen VLA collapse under visual/robot shift; recovery is
   policy-relative.
2. Benchmark protocol: forkable snapshots, episode/task-disjoint splits, paired
   arms, cost vectors.
3. Core positive evidence: W7/W8 recoverability matrix.
4. Mechanism negatives: candidate/temperature/short-replan/timing/selector.
5. Open problem: temporally extended recovery and safe handback (PRE-A2 signal;
   PRE-A3 confirmatory pending).
6. Release: configs, splits, artifacts, env locks, negative results.

## If PRE-A3 later passes

Add one method section after mechanism negatives:

- problem reformulation to minimum sufficient recovery duration + safe handback
- PRE-B dataset
- calibrated termination vs fixed-h / random / always-OFT
- Pareto and calibration
- still no world model unless residual gap is proven
