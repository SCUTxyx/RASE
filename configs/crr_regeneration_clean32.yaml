version: 1
run_name: crr_regeneration_clean32
mode: smolvla-screen

pool: runs/rase_ui_phase0g_independent48_pool
candidates_dir: runs/crr_regeneration_clean32_candidates_v1
output_dir: runs/crr_regeneration_clean32_screen_v1

sample:
  strategy: explicit_frozen_json

protocol:
  phase: crr_regeneration_eligibility
  cohort: clean_goal_long_runtime_states
  continuation_seed_mode: common_root_rollout
  expected_n_states: 32
  selection_uses_outcomes: false
  report: one_shot_candidate_hits
  note: >-
    Metadata-only clean Goal/Long slice, all pre-frozen steps 0/2/4/6.
    Common continuation RNG is paired within each root and repeat.

candidates:
  k: 4
  chunk_length: 10
  action_dim: 7
  temperature: 0.7
  base_seed: 2026082003
  policy_hash: 71d9563c8295284acba8fc2d5c19de000d6fe9ba58a406832af7ef3d221ed52f

adapter:
  policy_path: ckpts/smolvla_libero
  tokenizer_path: ckpts/SmolVLM2-500M-Instruct
  device: cuda
  num_steps: 10
  n_action_steps: 10
  observation_height: 360
  observation_width: 360
  continuation_temperature: 0.5

adaptive:
  first_stage_rollouts: 1
  total_rollouts: 1

scheduler:
  max_attempts: 3
  lease_seconds: 3600
  worker: crr-regeneration-clean32
