# RASE 下一步执行规划（2026-08-21，E3-B FAIL 之后）

> 上游：G0(嵌套) → G1(噪声无语义) → G2a/b(跨架构嵌套) → E3(教师强制分布偏移)
> → E3-B(DAgger 修好偏移，但教师天花板 + 剂量 + 泛化三嫌疑)。oracle gain 恒 0pp。
> 原则：诊断先行、预算封顶、论文轨并行、Gate 纪律不变。

---

## 0. 现状一句话

residual 路线方向仍保留，但**下一轮实验前必须先分清三个瓶颈**（教师天花板 /
部署剂量 / 状态可救性）——它们决定 E3-C 的形态；同时**论文轨立即启动**，
因为证据链已足以支撑独立产出，不依赖 E3-C 成败。

---

## 1. Track A：三诊断（第 1 天，~1h GPU + 纯分析，并行）

| 诊断 | 做法 | 判定阈值 | 产物 |
|---|---|---|---|
| **D1 教师天花板** | B2 的 24 roots 上跑 OFT direct recovery（复用 G2b `rollout_lerobot_direct_from_pool`，~40 min GPU） | ≥70% → 天花板排除；≈2/6 水平 → **坐实** | per-root teacher 成功率 |
| **D2 部署剂量** | B2 collector 的 activation diagnostics / one-shot 臂重放，统计实际 \|delta\| 分布 | \|delta\|≈0 → 门控/塌缩问题；≈0.08 → 8 步剂量不足 | delta 幅值分布 |
| **D3 可救性上限** | 纯分析：16 neither vs 8 both 状态的 GT 物体位姿→目标位姿距离分布；必要时 privileged 单次探针 rollout | neither 距离显著更大/不可达 → **状态本质不可救** | 可救性分层表 |

**D 系列 Gate**：三结果汇总成一张"瓶颈定位表"，决定 E3-C 形态；任何单一诊断
不阻塞其他两路。

## 2. Track B：论文轨（第 1 天起并行，零 GPU，不依赖 E3-C）

**论文主线**（六轮排除链 + 定理实证 + 文献空白）：

```text
贡献 1：Candidate Qualification 协议（E0→E3 gate 体系 + 预算纪律）
贡献 2：失败模式分类学（嵌套 / 噪声轴 / 教师天花板 三机制 + 判定实验）
贡献 3：Same-root 反事实数据集协议（文献确认无先例）+ 支配量化
        （ΔV(s) OPE 化、double-fault 度量、Experts in an MDP 实证）
贡献 4：可救性上限测量方法（D3 的形式化）
```

**立即动作**：整理 G0→E3-B 全部 artifact 为论文目录结构（报告、JSON、图），
补两个廉价形式化实验（纯 CPU/已有数据）：
- H_within 统计检验的形式化（bootstrap + 幂分析，说明"0 是测出来的不是噪声"）；
- 支配度量的定义与跨协议一致性表（H_within、oracle gain、double-fault、neither 占比）。

**时间预算**：2-3 周成稿。**这是主轨道**——E3-C 成功则插入"positive result"
章节；失败则论文直接成立。

## 3. 诊断后分叉（第 2 天决策）

| D1 教师 | D2 剂量 | D3 可救性 | 结论 | E3-C 形态 |
|---|---|---|---|---|
| 高(≥70%) | ≈0 | 可救 | 表达力/门控问题 | **E3-C1 剂量修正**：one-shot 扩为 2-3 chunk 纠正窗 + 输入加冻结视觉特征（SmolVLM2 已就位） |
| 低(≈33%) | 任意 | 可救 | 教师天花板 | **E3-C2 势场监督**：GT 物体位姿→目标位势场，residual 监督=降低势场（比 RL 便宜、信息量大于 proprio） |
| 任意 | 任意 | **不可救** | 状态本质无解 | **换任务集**（筛"可救失败状态占比高"的任务）或 **直接转论文**（residual 路线在 LIBERO 冻结） |
| 低 | ≈0 | 不可救 | 三重叠加 | 转论文（不投算力） |

**E3-C 统一预算上限**：采集+训练+复测 ≤ 2 天 GPU；**一轮** B2 Gate 复测
（24 roots 任务隔离，同 gate：source-only>0 ∧ residual-only>0 ∧ H_within≥5%
∧ oracle gain≥5pp ∧ 跨 ≥2 tasks）。

## 4. E3-C PASS 后的恢复路线（不投入当前预算）

```text
冻结模型+超参 → PRE-A3 validation → RASE relative verifier（same-root）
→ action-shuffled / label-permuted / state-only 对照 → conformal abstention
→ risk-guided regeneration 闭环（主 endpoint：Gain≥3pp 且 CI_lower>0）
```

## 5. 时间线

| 周 | Track A/B | 里程碑 |
|---|---|---|
| 第 1 天 | D1+D2+D3 并行 | 瓶颈定位表 + 论文骨架 |
| 第 2-4 天 | E3-C 采集+训练（按分叉形态） | 训练诊断数字 |
| 第 5-7 天 | E3-C 复测 B2 Gate | PASS→恢复主线；FAIL→冻结 residual |
| 第 2-3 周 | 论文成稿（并行） | 无论 E3-C 成败均成立 |

## 6. 停止纪律（写死）

1. E3-C **一轮** FAIL → 不再扩 K/调参/换监督 → 冻结 residual 路线。
2. 不在 oracle gain=0 时训练 verifier / 调 margin / 跑闭环。
3. 不把任务级路由包装成 state-level 仲裁主张。
4. 论文轨不因 E3-C 推迟；E3-C 结果只决定论文是否有 positive 章节。
5. GPU 用完即释放；所有 run 落盘 + git 快照（沿用 df54eed 纪律）。

---

## 7. 立即执行清单（第一批）

```text
Track A（并行）：
  D1: 复用 rollout_lerobot_direct_from_pool → runs/e3b_b2_oft_teacher_direct_v1/
  D2: 分析 B2 collector activation diagnostics（或重放 one-shot 臂）
  D3: 解析 B2 roots 的 GT 物体位姿（same_root/future_objects 机制）→ 距离分布表

Track B（并行）：
  论文目录：progress/paper_negative_results/（G0→E3-B 报告 + JSON + 图）
  两个廉价形式化实验（纯 CPU）
```
