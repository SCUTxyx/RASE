# NGC W5 L1–L2 camera/robot 小状态池采集

| 项 | 内容 |
|---|---|
| 记录时间 | **2026-07-27 18:43 CST** |
| 状态 | **采集完成（40/40 episode）；尚未做 screen** |
| 问题 | 现 scale200 池仅有 L3–L5；能否采到更轻扰动态，供 SmolVLA 可救边界再筛？ |
| 配置 | [`configs/collect_w5_l1_l2_camera_robot.json`](../configs/collect_w5_l1_l2_camera_robot.json) |
| Policy | 冻结 `ckpts/smolvla_libero` + `ckpts/SmolVLM2-500M-Instruct` |
| LIBERO-Plus | `/root/autodl-tmp/src/LIBERO-plus` |
| 产物 | [`pool/ngc_w5_l1_l2_camera_robot/`](../pool/ngc_w5_l1_l2_camera_robot/) |
| 母记录 | [W5 screen 网格](2026-07-27_ngc_w5_smol_screen_t07.md) · [scale200 池](2026-07-18_ngc_step1_scale200_camera_heavy.md) |

---

## 1. 一句话结论

已采完 **40 episode** 的 camera/robot **L1–L2** 小池，manifest **762** 态，目前抽查/计数均为 **failure**（与 Plus 上 SmolVLA 极低成功率一致）。本记录只覆盖采集；**L1–L2 screen 尚未开始**。

---

## 2. 配置要点

| 项 | 值 |
|---|---|
| episodes | 40 |
| dims | camera 50% / robot 50%（配额） |
| levels | camera/robot ∈ {1, 2} |
| suites | Long/Goal/Spatial/Object 配额 |
| snapshot cadence | 每 2 action chunks |
| success retention | 0.20（若 episode 成功才抽样保留） |

启动时曾因 `policy_path=null` 报错；已写入显式 checkpoint / tokenizer / LIBERO-Plus 路径后重跑成功。

---

## 3. 终态（采集）

| 指标 | 值 |
|---|---|
| episode 目录 | **40** |
| manifest states | **762** |
| outcomes（manifest） | failure **762** / success **0**（当前计数） |
| `.collect_current_episode.json` | 无（进程已结束） |
| `summary.json` | 未写出（pipeline 未落该文件；以 manifest 为准） |

墙钟约 **17:56–18:15 CST**（与 CareVLA 同卡抢占，仅供参考）。

---

## 4. 意义与下一步

- **意义**：把「SmolVLA 在 L3–L5 全灭」从「从未测过容易档」里拆开；若 L1–L2 screen 仍≈0，可更有把握转向 OFT fallback。  
- **下一步**（需手动）：

```bash
# 先按 W5 failure 配置改 pool 路径或新建 l1l2 screen 配置后：
python scripts/sample_state_keys.py --config <l1l2_screen_yaml> --inventory-only ...
python scripts/sample_state_keys.py --config <l1l2_screen_yaml> --output runs/ngc_w5_l1_l2_state_keys.json
# 再 generate candidates + smolvla-screen
```

在写出 L1–L2 screen 结果前，不要更新「SmolVLA 全局不可救」的最终判断。
